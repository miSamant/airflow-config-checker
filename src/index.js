import React from 'react';
import ReactDOM from 'react-dom/client';
import AirflowConfigCalculator from './Airflow-Calculator';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AirflowConfigCalculator />
  </React.StrictMode>
);
